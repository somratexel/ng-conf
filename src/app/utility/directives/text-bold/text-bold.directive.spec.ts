import { TextBoldDirective } from './text-bold.directive';

describe('TextBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new TextBoldDirective();
    expect(directive).toBeTruthy();
  });
});
